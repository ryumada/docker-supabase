# Find 'IMPORTANT' to change the important variable and 'change' to change variables are recommended to change.

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------
# !!! IMPORTANT: Change this password in production !!!
POSTGRES_PASSWORD=your-super-secret-and-long-postgres-password
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_HOST=db
POSTGRES_VERSION=15.8.1.085

# ------------------------------------------------------------------------------
# Auth Configuration
# ------------------------------------------------------------------------------
# !!! IMPORTANT: Use a secure random secret (at least 32 chars) !!!
JWT_SECRET=your-secret-jwt-token-at-least-32-characters-long
JWT_EXPIRY=3600

# !!! IMPORTANT: These keys must be generated based on your JWT_SECRET !!!
ANON_KEY=your-anon-key
SERVICE_ROLE_KEY=your-service-role-key

ADDITIONAL_REDIRECT_URLS=
DISABLE_SIGNUP=false
ENABLE_EMAIL_SIGNUP=true
ENABLE_EMAIL_AUTOCONFIRM=false
ENABLE_PHONE_SIGNUP=true
ENABLE_PHONE_AUTOCONFIRM=false
ENABLE_ANONYMOUS_USERS=false
# This should match SUPABASE_PUBLIC_URL
API_EXTERNAL_URL=http://localhost:8000

# ------------------------------------------------------------------------------
# SMTP Configuration (used by Auth service)
# ------------------------------------------------------------------------------
SMTP_ADMIN_EMAIL=admin@example.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_SENDER_NAME=Supabase Auth

MAILER_URLPATHS_INVITE=/auth/v1/verify
MAILER_URLPATHS_CONFIRMATION=/auth/v1/verify
MAILER_URLPATHS_RECOVERY=/auth/v1/verify
MAILER_URLPATHS_EMAIL_CHANGE=/auth/v1/verify

# ------------------------------------------------------------------------------
# Studio Configuration
# ------------------------------------------------------------------------------
STUDIO_DEFAULT_ORGANIZATION=Default Organization
STUDIO_DEFAULT_PROJECT=Default Project
# change this port if 3000 is already in use
STUDIO_PORT=3001
# !!! IMPORTANT !!!
# If you want to access Supabase from other devices on your network (like your phone),
# change 'localhost' to your LAN IP address (e.g. http://192.168.1.15:8000)
SUPABASE_PUBLIC_URL=http://localhost:8000
OPENAI_API_KEY=

# ------------------------------------------------------------------------------
# Pooler (Supavisor) Configuration
# ------------------------------------------------------------------------------
# change this ports if 6432 is already in use
POOLER_PORT=6432
POOLER_PROXY_PORT_TRANSACTION=6543
POOLER_TENANT_ID=stub
POOLER_DEFAULT_POOL_SIZE=20
POOLER_MAX_CLIENT_CONN=100
POOLER_DB_POOL_SIZE=20

# !!! IMPORTANT: Use a secure random secret (at least 64 chars) !!!
SECRET_KEY_BASE=your-secret-key-base-at-least-64-characters

# !!! IMPORTANT: Use a secure random secret (at least 32 chars) !!!
VAULT_ENC_KEY=your-vault-enc-key-at-least-32-characters

# ------------------------------------------------------------------------------
# Kong Configuration
# ------------------------------------------------------------------------------
# change this port if 8000 and 8443 is already used
KONG_HTTP_PORT=8000
KONG_HTTPS_PORT=8443
DASHBOARD_USERNAME=supabase
# !!! IMPORTANT: Change this password in production !!!
DASHBOARD_PASSWORD=this_is_not_a_password

# ------------------------------------------------------------------------------
# Analytics Configuration
# ------------------------------------------------------------------------------# Analytics
# Secure:
LOGFLARE_PUBLIC_ACCESS_TOKEN=your-logflare-public-access-token
LOGFLARE_PRIVATE_ACCESS_TOKEN=your-logflare-private-access-token
# change this port if 4000 is already used
LOGFLARE_PORT=4000
GOOGLE_PROJECT_ID=
GOOGLE_PROJECT_NUMBER=

# ------------------------------------------------------------------------------
# General
# ------------------------------------------------------------------------------
# The URL of your frontend application
# If testing on a mobile device, set this to your LAN IP (e.g. http://192.168.1.15:3000)
SITE_URL=http://localhost:3000
CLIENT_PORT=3000

# ------------------------------------------------------------------------------
# Edge Functions Configuration
# ------------------------------------------------------------------------------
FUNCTIONS_VERIFY_JWT=true

# ------------------------------------------------------------------------------
# Additional Infrastructure
# ------------------------------------------------------------------------------
# !!! IMPORTANT: Use a secure random secret (at least 32 chars) !!!
PG_META_CRYPTO_KEY=your-crypto-key-at-least-32-characters

PGRST_DB_SCHEMAS=public,storage,graphql_public
IMGPROXY_ENABLE_WEBP_DETECTION=false
DOCKER_SOCKET_LOCATION=/var/run/docker.sock
PWD=.
